# --- Mock mode (defaults to true) ------------------------------------
# When true, the CRM renders using mock data — no backend needed.
# Set to "false" once you have the FastAPI backend running.
NEXT_PUBLIC_USE_MOCK=false

# --- FastAPI backend URL (only needed when USE_MOCK=false) ------------
NEXT_PUBLIC_API_URL=http://localhost:8000
